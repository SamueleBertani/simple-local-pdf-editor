/** Minimum allowed zoom scale (50%) */
export const MIN_SCALE = 0.5;

/** Maximum allowed zoom scale (300%) */
export const MAX_SCALE = 3;

/** Step size for zoom in/out buttons */
export const SCALE_STEP = 0.25;

/** Default zoom scale (100%) */
export const DEFAULT_SCALE = 1;

/** Sensitivity multiplier for Ctrl+Wheel zoom gesture */
export const WHEEL_SENSITIVITY = 0.01;
